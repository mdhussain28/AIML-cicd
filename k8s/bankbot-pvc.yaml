apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bankbot-data-pvc
  namespace: ai-production
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 1Gi
